# CURRENT RESEARCH FRONTIERS

Last reconciled: 2026-08-30

Purpose: current-state locator for Gate 1B and Erdős #287. This is not itself a proof. Historical provenance belongs in the append-only proof indexes and dated reports.

---

## Gate 1B

**Status:** `GATE1B OPEN`.

### Safe closed range

The upper-`D` analytic range remains closed:

```math
D\ge Y^{3/4}L^{-B_*}.
```

The lower-`D` range remains open.

### Stable source/geometry bank

The following reductions survive hostile audit:

- centered `q,v` source splice repaired in augmented frequency coordinates;
- exact same-`u` physical AP coordinates;
- the `2\times2` modulo-`\ell` system for `(Z_-,Z_+)` has unit determinant;
- fixed-`\ell` physical `(Z_-,Z_+)` pair is unique in the short `Z` boxes;
- quotient `(r,k)` normal form is exact;
- AP-sampling multiplicity second moment is proved:
  ```math
  \sum_{\mathbf z}m(\mathbf z)^2\ll Y^8D^{-2}L^C;
  ```
- on the cross-`u` geometry, `(q,u_i)=1` and
  ```math
  \ell_1\equiv\ell_2\pmod{(u_1,u_2)}
  ```
  are source-exact algebraic facts.

### First exact source residual

The newest hostile audit retracts the attempted same-`u` closure.

For the leaf `j=1` (pure defect coordinates), the required `\alpha_j,\gamma_j` eighth moments pass. For `j=2,3,4,5`, the model coordinates `\lambda_i` are missing the literal weighted high-moment theorem

```math
\sum_{n\asymp Y}\tau(n)^7|\lambda_i(n)|^8\ll YL^C.
```

Therefore the first source residual is

```text
C4SHIFT-BEZOUT-SOURCE-ALPHAGAMMA-L8-45
```

with status `PARTIAL PASS / SOURCE PIN`.

### First exact analytic/compiler residual after that source pin

The previously claimed diagonal closure used an invalid compiler splice. The old safe quantity

```math
\mathcal N_D^{\rm safe}\ll Y^{9/8}D^{-1/2}L^C
```

is an `L^1_\theta\ell_v^2` norm. It cannot be squared and promoted into a Plancherel `L^2_\theta\ell_v^2` energy estimate. In addition, the old `\alpha,\gamma` block had already been compressed into a source `\Gamma`-norm before the attempted AP-sampling replacement.

The claimed estimate

```math
\mathcal E_{\rm diag}(D)\ll Y^{3/2}D^{-2}L^C
```

is therefore withdrawn.

The first analytic/compiler child is now

```text
C4SHIFT-BEZOUT-
LOWERD-SAMEU-
SOURCEDIAGONAL-NORMALIZATION45
```

whose required theorem is a **direct** normalized positive Plancherel-energy estimate from the literal physical source, with AP sampling inserted before global Cauchy/source compression.

### Same-`u` dependent children

Until the direct source diagonal is closed:

```text
SAMEU-H0-SOURCEWEIGHTED-PRODUCTENERGY45: SOURCE PIN
SAMEU-HNE-SHIFTEDCORRELATION45: SOURCE PIN
SAMEU-PHYSICAL-AP-GRAM45: OPEN
```

The missing `H0` theorem is a source-weighted physical-fibre moment retaining the synchronized `X_+`, `Z_+`, AP selector, `g` fibre, and packet data. The `HNE` shifted-correlation step is conditional on that physical-fibre theorem plus a bounded packet-shift incidence bound.

### Cross-`u`

Analytic cross-`u` work is deliberately not promoted while same-`u` remains open. The banked algebra only is:

```math
(q,u_i)=1,
\qquad
c=(u_1,u_2)\gg T,
\qquad
\ell_1\equiv\ell_2\pmod c,
\qquad
\ell_1-\ell_2=cj.
```

`J0`, `JNE`, and the source-weighted mixed Gram remain unreached analytically in the current depth-first route.

### Parallel local residual

Independently retain

```text
TOPBAND-BROAD-MAJOR-TREE-MATCH45
```

as the parallel local/source owner.

### Current Gate 1B ledger

| Node | Status |
|---|---|
| Upper-`D` range | CLOSED |
| centered `q,v` source splice | PASS |
| same-`u` `2x2` AP residue geometry | PASS |
| AP-sampling multiplicity second moment | PASS |
| `alpha/gamma` L8 | `j=1` PASS; `j=2..5` SOURCE PIN |
| same-`u` source diagonal normalization | OPEN |
| H0 physical-fibre product energy | SOURCE PIN |
| HNE shifted packet correlation | SOURCE PIN |
| same-`u` physical AP Gram | OPEN |
| cross-`u` common-`q` congruence geometry | PASS ALGEBRA |
| cross-`u` analytic mixed Gram | NOT REACHED / OPEN |
| parallel top-band local owner | OPEN |
| Gate 1B | OPEN |

### Next action

First recover/prove the `\lambda_i` weighted high moments, then rebuild the **literal positive same-`u` Plancherel diagonal from zero**, inserting AP sampling before any global source compression. Only after the direct normalization, H0 physical fibre, and HNE packet-shift incidence pass should the route return to cross-`u`.

---

## Erdős Problem #287

**Status:** `ERDOS287 OPEN`.

### Formal/source first residual

The first sequential source pin remains

```text
287-K0-SP2-DET1-
SHAREDGCD-OMEGAH-
NORMALISATION-SOURCEPIN45
```

The exact `\lambda_H=\mu*\Omega_H` projector identities are banked, but the literal `\Omega_H(e)=W(e/H)` normalisation/support/packet constants remain source-pinned. Polynomial-`B` / SMALL-`B` and polynomial-`G` margins remain parallel source-margin owners where invoked.

### Hybrid 2

The local one-sided `\Delta`–Möbius reciprocal theorem has survived hostile audit and source repair:

```text
DET1-FUSED-
DELTA-MOBIUS-RECIPROCAL-
HYBRID2-45: PASS
```

The repaired bank includes:

```text
SHORT-D: CLOSED
SHORT-M: CLOSED
SHORT-Q: CLOSED via SHORT-Q ⊂ SHORT-M
large d2: CLOSED
```

as well as

```math
g_0g_0'\mid e(b_2-b_1),
\qquad
|\beta|\ll(\log\log X)^2=L^{o(1)},
\qquad
\lambda_{\rm arch}=O(1).
```

The fixed smooth Archimedean separated packet and standard additive separated-frequency large sieve provide the local analytic inputs. `\Omega_H` remains an outer compiler/source pin, not a local Hybrid-2 dependency.

### Current `b_1\ne b_2` analytic residual

Let

```math
D_b=b_2-b_1,
\qquad
z=(b_1,m_2),
\qquad
d_2=(m_2,D_b).
```

The audited long-edge conductor factors as

```math
\boxed{Q_1^\circ=z^2q_Cq_m},
```

with

```math
q_C
=2\frac e{(e,N_1)}
  \frac{r_2}{(r_2,D_b)}
  b_1^\flat,
\qquad
q_m=m_2^\flat.
```

Short `q_C` and short `q_m` are routed by the existing cross-gcd / large-`d_2` / SHORT-`M` / SMALL-`B` owners. The survivor has all conductor factors long and retains the ultra-near restriction

```math
\mathcal D
\ll
eB L^C
\sqrt{\frac{R_{\rm aff,0}}{g_0g_0'}}.
```

The simultaneous reciprocal phases leave a genuinely moving matrix coefficient: the `q_m` phase sees `\Delta D_b/d_2`, while the `q_C` phase still depends separately on `\Delta`, `b_1`, and `m_1`.

Thus the first analytic residual, conditional on the outer source normalisations, is

```text
287-K0-SP2-DET1-
FUSED-SMALL-AFFINEDEFECT-
COMMONE-SMALLH-
HYBRID2-LONGEDGE-
THREEFACTOR-MOVINGMOD-
MATRIX45
```

The missing mechanism is a source-specific two-parameter reciprocal large-sieve / moving-modulus matrix estimate for the simultaneous `q_C` and `q_m` phases while preserving the opposite Möbius sign.

### Parallel `b`-diagonal child

```text
287-K0-SP2-DET1-
FUSED-SMALL-AFFINEDEFECT-
BDIAGONAL-LONGQC-
PRODUCTMOD-RECT45
```

remains open. Its short-`q_c` edge is routed to SMALL-`B`; the earlier character-fourth-moment route is natural scale only.

### Current Erdős 287 ledger

| Node | Status |
|---|---|
| CASE-B primitive-fraction formal bank | BANKED / CONDITIONAL WHERE EXPLICIT |
| `Omega_H` normalization | SOURCE PIN |
| `B` / SMALL-`B` margin | PARALLEL SOURCE PIN / ROUTER |
| `G` polynomial margin | PARALLEL SOURCE PIN / ROUTER |
| complete one-sided conductor | PASS |
| Hybrid 2 | PASS |
| SHORT-D / SHORT-M / SHORT-Q | CLOSED |
| `g0 g0' | e(b2-b1)` | PASS |
| three-factor long-edge reduction | PASS / STRICT REDUCTION |
| three-factor moving-modulus matrix | OPEN ANALYTIC |
| `b`-diagonal long-`q_c` product-modulus rectangle | OPEN ANALYTIC |
| Erdős #287 | OPEN |

### Next action

Do not reopen generic Hybrid 2. First hostile-audit the exact `z^2q_Cq_m` factorisation and its three phases. Then test reciprocal fusion of the `q_C` and `q_m` components; if fusion fails, expose the genuine two-parameter frequency in `(\Delta,\Delta D_b/d_2)` and attack the resulting source-weighted moving-modulus matrix directly.

---

## Mandatory update rule

Whenever either programme changes frontier, update this file and append the corresponding proof index / dated snapshot with status, parent/children, deriving run, hostile-audit status, Lean provenance if banked, supersessions/retractions, key equations, current/required bounds, missing mechanism, and next attack. Never delete historical provenance.
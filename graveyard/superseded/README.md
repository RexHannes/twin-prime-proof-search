# Superseded proof coordinates

The following are no longer the preferred controlling decomposition:

- old FSHC Sector A/B/C architecture;
- separate `R_minor` / high-frequency child bookkeeping where absorbed by a later signed parent;
- audited `R_hi` as the sole public-facing frontier **only in the newer post-R11 candidate map**, which recompiles the physical high-frequency source into `R_*` and the two-copy square.

Important evidence firewall:

```text
Audited R11 release: R_hi remains OPEN.
Post-R11 live map:   R_hi is candidate-recompiled into R_*.
```

Therefore this directory records a change of controlling coordinates, not a retroactive rewrite of the audited manuscript.

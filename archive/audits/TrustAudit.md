# Trust Audit

## Trust rules

The new NANC bank has no proof placeholders, global assumptions, unsafe
declarations, or alternative unchecked implementations. Search hits elsewhere
in the historical project, if any, are comments explaining prohibitions or
status prose rather than active declarations.

## Verification

`lake build RequestProject.NANCBank` and the full default build were run. The
proof-carrying analytic accessors merely project proofs explicitly supplied in
records. The dependency assemblies derive named conclusions only from explicit
premises; none concludes `True` as a surrogate for an analytic result.

Axiom inspection of the new accessors introduces no axioms. Type-A theorems use
only ordinary Lean/Mathlib foundations.

## Open-theorem audit

No open analytic theorem was promoted. Full ROW, SAME-R, CDV, DIFF-R, Ford Type
II, FCPT, twin primes, and Hardy-Littlewood remain open or conditional exactly as
recorded in `CurrentBankStatus.lean`.

## Final verdict

LEAN_BANK_COMPLETE_FOR_CURRENT_TYPE_A_CHECKPOINT

## W4Frontier supplement

- No new analytic theorem is claimed.
- FCPT remains open.
- Salié identity/local transform are explicitly marked external proof-carrying
  algebraic interfaces; the project has no global inhabitants for them.
- The signed joint-hit determinant census remains OPEN.

The active determinant graph is

    pq'uv' - p'qu'v = 2kz,

with delta = kr and m' = m + delta.

The former formula with RHS 2delta z was false: it contained
an extra factor r. Algebraically,

    r(2kz) = 2delta z.

The signed joint-hit census remains OPEN.

- The active determinant graph is `pq'uv' - p'qu'v = 2kz`, with
  `delta = kr` and `m' = m + delta`.
- RETIRED_FALSE_GRAPH: the former RHS `2delta z` contained an extra factor
  `r`; the checked relation is `r(2kz) = 2delta z`.
- The exponent correction from `24 + 28 = 52` to `4 + 28 = 32` is finite
  arithmetic only and does not close the generic signed census or any
  exceptional analytic fibre.
- Retired routes are status entries and remain visible in the bank history.
- No proof placeholder, global assumption declaration, unsafe declaration, or
  unchecked alternative implementation was introduced.

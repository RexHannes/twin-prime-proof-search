# CURRENT GATE ARCHITECTURE AND STATUS — TWIN PRIME PROGRAMME

**Checkpoint:** 1 September 2026  
**Headline:** `TWIN PRIME CONJECTURE: OPEN`.

This is the living architecture note for the fixed-shift / Ford–Maynard / Twin-Prime programme. Historical mixed-project versions remain in Git history but do not control the current tree.

## 1. Gate 0

**Role:** finite/source partition and packet generation.

**Status:** `OPEN`.

The existing finite banks are reused. An exhaustive literal source-partition/census must be supplied before any downstream gate requirement is promoted from architecture to theorem.

## 2. Gate 1A

**Role:** provider layer for the direct/all-`m` packet families demanded by the literal fixed-shift census.

**Status:** `OPEN` in the current living ledger.

Substantial kernel-checked finite/algebraic provider machinery is preserved. Source/interface obligations remain explicit; high-conductor component savings must not be promoted to a whole routed piece without the corresponding compiler.

## 3. Gate 1B

**Role:** provider layer for the determinant-2 / rank-one / generated fixed-shift family.

**Status:** `OPEN` globally.

The current living frontier is in [`RESEARCH_FRONTIERS/CURRENT_FRONTIERS.md`](RESEARCH_FRONTIERS/CURRENT_FRONTIERS.md).

Safe bank includes exact source/geometry and finite Fourier components. The current source/analytic sequence is:

```text
C4SHIFT-BEZOUT-SOURCE-ALPHAGAMMA-L8-45
    PARTIAL PASS / SOURCE PIN
        |
        v
C4SHIFT-BEZOUT-LOWERD-SAMEU-SOURCEDIAGONAL-NORMALIZATION45
    OPEN
        |
        +--> SAMEU-H0-SOURCEWEIGHTED-PRODUCTENERGY45 : SOURCE PIN
        +--> SAMEU-HNE-SHIFTEDCORRELATION45           : SOURCE PIN
        +--> SAMEU-PHYSICAL-AP-GRAM45                 : OPEN
```

The upper-`D` range remains banked; the lower-`D` direct source diagonal must be proved from the literal positive Plancherel source rather than by squaring an `L^1_θ ℓ_v^2` bound.

## 4. Gate 2

**Role:** reassemble the provider outputs into the actual fixed-shift Type-II statement required downstream.

**Status:** `CONDITIONAL COMPILER`.

No stronger Type-II target is created merely because a local Gate-1A or Gate-1B child closes.

## 5. Ford-generated source / packet census

The Ford–Maynard source grammar and packet census are separate source objects. Published/source definitions must be supplied literally; missing source data are not reconstructed from memory or from another project.

Provider arrows from the census to Gate 1A / Gate 1B are therefore census-dependent.

## 6. Full Ford–Maynard Type II

```text
FULL_FM_TYPEII: OPEN
```

A local/canonical generated Type-II estimate is not silently identified with the full global hypothesis package.

## 7. Twin-prime conclusion

```text
GATE1B: OPEN
FULL_FM_TYPEII: OPEN
TWIN_PRIME_CONJECTURE: OPEN
```

No Hardy–Littlewood asymptotic is claimed.

## 8. Formal bank

The living formal status is machine-readable in:

- `RequestProject/CurrentProgramme/CurrentStatus.lean`
- `RequestProject/CurrentProgramme/DependencyGraph.lean`

The current dependency graph is project-local:

```text
Gate 0
  -> packet census
      -> Gate 1A
      -> Gate 1B
  -> Gate 2
  -> Full Ford–Maynard Type II
  -> Twin Prime
```

All analytic/source interfaces remain explicit. Kernel-proved finite identities do not by themselves inhabit analytic inputs.

## 9. Project firewall

The living Twin-Prime status, frontier and dependency graph contain no active source/status/compiler node owned by the separate Erdős-#287 programme. Generic mathematical lemmas may be reused only as generic lemmas; they carry no cross-project theorem ownership.

See [`PROJECT_SCOPE.md`](PROJECT_SCOPE.md).

# CURRENT PROGRAMME — TWIN PRIME MASTER CONTINUATION REPORT

**Checkpoint:** 1 September 2026.

This is the living continuation report for `TwinPrimeProject.CurrentProgramme`. Historical mixed-project reports are retained in Git history; this current file is scoped only to the fixed-shift / Ford–Maynard / Twin-Prime programme.

Machine-readable current counterparts:

- `RequestProject/CurrentProgramme/CurrentStatus.lean`
- `RequestProject/CurrentProgramme/DependencyGraph.lean`
- `RequestProject/CurrentProgramme/AxiomAudit.lean`
- `CURRENT_GATE_ARCHITECTURE_AND_STATUS.md`
- `RESEARCH_FRONTIERS/CURRENT_FRONTIERS.md`

If prose and the current Lean status ledger disagree about a formal-status label, the Lean ledger controls.

## Current headline status

```text
GATE0: OPEN
GATE1A: OPEN
GATE1B: OPEN
GATE2: CONDITIONAL COMPILER
FULL_FM_TYPEII: OPEN
TWIN_PRIME_CONJECTURE: OPEN
```

## Current Gate-1B focus

The current living frontier preserves the upper-`D` bank and exact source/geometry reductions. The controlling sequence is:

```text
C4SHIFT-BEZOUT-SOURCE-ALPHAGAMMA-L8-45
    PARTIAL PASS / SOURCE PIN

C4SHIFT-BEZOUT-LOWERD-SAMEU-SOURCEDIAGONAL-NORMALIZATION45
    OPEN

SAMEU-H0-SOURCEWEIGHTED-PRODUCTENERGY45
    SOURCE PIN

SAMEU-HNE-SHIFTEDCORRELATION45
    SOURCE PIN

SAMEU-PHYSICAL-AP-GRAM45
    OPEN
```

The lower-`D` same-`u` diagonal must be rebuilt directly from the literal positive Plancherel source. A previously available `L^1_θ ℓ_v^2` quantity is not promoted by squaring into an `L^2_θ ℓ_v^2` energy theorem.

## Kernel-safe bank

The repository retains exact finite/algebraic banks for source geometry, line/Fourier algebra, status firewalls, source-census types and conditional compilers. Their role is to expose exactly which analytic/source premises are still missing.

No analytic input is to be constructed by `Classical.choice`, an artificial default instance, or a custom axiom.

## Provider architecture

The current project-local architecture is:

```text
finite source / Gate 0
    -> generated packet census
        -> Gate 1A
        -> Gate 1B
    -> Gate 2
    -> Full Ford–Maynard Type II
    -> Twin Prime
```

The literal census decides which provider arrows are actually required. An architectural arrow is not itself a proof that the source demands that gate.

## Reuse rule

Generic mathematical identities may be reused across research programmes only as source-neutral lemmas. A source-specific status, packet owner, analytic input, final compiler or problem conclusion from a separate project is not an admissible dependency here.

## Source discipline

Published Ford–Maynard source data and physical packet definitions must be transcribed literally where required. Missing definitions are not reconstructed from memory, a prompt, or another repository.

## Publication firewall

The following remain nonclaims:

- no unconditional/global Gate-1A closure is inferred from a local provider bank;
- no unconditional/global Gate-1B closure is inferred from a local/canonical child;
- no local generated Type-II theorem is silently identified with Full Ford–Maynard Type II;
- no Twin-Prime proof or Hardy–Littlewood asymptotic is claimed.

## Project separation

The current source tree, living status ledger and dependency graph are maintained as Twin-Prime-only objects. Separate-problem research may remain visible in immutable Git history but is not part of this current theorem graph.

See `PROJECT_SCOPE.md` for the explicit scope rule.

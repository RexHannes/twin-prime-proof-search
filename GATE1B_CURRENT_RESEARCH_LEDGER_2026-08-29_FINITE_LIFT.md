# Gate 1B current research ledger — finite-lift frontier

Date: 29 August 2026

This is an append-only research/status ledger. It does not overwrite historical Lean status layers and does not claim Gate 1B closed.

## Current controlling research frontier

`GATE1B: OPEN`.

Latest Pro narrowing:

- `DETLINE-CONDUCTOR-LE-Y45`: power closed.
- `DETLINE-LARGE-LIFT-DISPERSION45`: CLOSED at current research/paper level.
- `DETLINE-CONDUCTOR-LOCAL-G-ENERGY45`: exact fixed-lift form / partial gain.
- `DETLINE-LIFT-CREATES-NEW-CONDITION45`: NO; lift does not create an independent determinant relation.
- `DETLINE-FINITELIFT-NEARPRIM-REDUCTION45`: PASS.
- `DETLINE-HIGHCOND-DENSE-SATURATION-EXCLUSION45`: closed for log-large lifts, open for finite lifts.
- `DETLINE-HIGHCOND-BETA-RHO-CROSSPAIR45`: PARTIALLY CLOSED / STRICTLY REDUCED.
- `TOPBAND-BETA-BROADMINOR-DETLINE45`: large-lift conductor cells closed; finite-lift cells open.

Current first exact residual:

`DETLINE-NEARPRIM-FINITELIFT-DENSE-SATURATION45`.

Literal surviving range: for each requested `A`, after choosing a fixed polylog exponent `B_A`,

`1 <= e <= (log X)^(B_A)`, `ell = c e ~ R`, `c ~ R/e = R (log X)^(-O_A(1))`.

The required bound is an arbitrary-log improvement over the natural scale for the source-minimal pairing of the actual broad-minor remainder and actual determinant companion. At `e=1` the current conductor-cell bound is only natural scale `X (log X)^(C0)`; there is no power deficit to repair. The missing mechanism is source-specific relative phase decorrelation in near-primitive conductor blocks.

## Newly bankable research mathematics from the latest Pro run

For exact lift `e`, the conductor-local energies give schematically

- `E_G,2(e) << e^(-1) Y^(29/2) log^(O(1)) X`;
- `E_R,2(e) << R Y e^(-2) log^(O(1)) X` in the surviving `C>Y` range;
- hence `|T_j(e)| << X e^(-3/2) log^(O(1)) X`.

Therefore summing `e >= E` gives `<< X E^(-1/2) log^(O(1)) X`. Choosing `E=(log X)^(B_A)` closes every logarithmically large lift with arbitrary logarithmic saving. The argument is uniform in the five major-tree leaves `j=1,...,5`.

Small lifts only produce polylogarithmically many local additive/coprimality twists; mixed and two-copy lift dispersion return the existing determinant/rank-two relation and add no new rank.

## Supplied Aristotle run — formal bank evidence

The supplied Aristotle transcript records successful local construction/build/audit of substantial Gate-1B finite algebra before the run timed out during report writing. In particular it records:

- exact shifted determinant algebra and divisor-switch/solve forms;
- shift-independent congruences;
- determinant character transfer;
- exact finite band-to-shift Fourier identity and zero-shift firewall;
- shifted-MAM packet/source structures and uninhabited analytic sockets;
- source-minimal character pairing and Cauchy-strengthening firewall;
- shifted-MAM fivefold operator socket with explicit canonical local term;
- revised all-k conditional compiler;
- append-only `CurrentStatusHighKShift` and `CurrentStatusShiftedMAMOperator` layers;
- axiom-audit modules, with the transcript reporting only standard `propext`, `Classical.choice`, `Quot.sound` dependencies / no proof escapes in the new bank.

The transcript also records multiple `git commit` + `git push` operations. However, the currently connected public GitHub `main` does not independently expose those new commit messages at the time of this ledger update. Therefore these are recorded conservatively as:

`SUPPLIED ARISTOTLE RUN / LOCAL BUILD-AUDIT EVIDENCE / PUBLIC-SYNC NOT INDEPENDENTLY VERIFIED`.

The Aristotle run timed out when beginning the missing reports. The missing reports are bookkeeping/provenance work, not evidence that the already-built declarations failed.

## Formal status inherited from the supplied Aristotle operator layer

Safe supplied-layer statuses include:

- `DETERMINANT-CHARACTER-TRANSFER45`: Lean-proved in supplied run.
- `DETERMINANT-CHARACTER-TO-SHIFTED-MAM45`: proved finite in supplied run.
- `SOURCE-MINIMAL-CHARACTER-PAIRING45`: proved finite in supplied run.
- `ENDPOINT-CHAR-TWISTED-FACTORMOD-SQUARE45`: superseded as controlling frontier; stronger sufficient Cauchy socket; NOT false.
- `FIVE-DEFECT-RESIDUE-ADAPTER45`: source-open / uninhabited.
- `FINITE-SHIFT-STABILITY45`: proved algebraic.
- `POLYLOG-SHIFT-STABILITY45`: analytic-open.
- `SHIFTED-MAM-DIVISOR-SWITCH45`: proved algebraic.
- `SHIFTED-MAM-FIVEFOLD-OPERATOR45`: analytic-open at that formal abstraction layer; uninhabited.
- `MOTOHASHI-SHIFTED-MAM-DICTIONARY45`: false route only at the source-coupled operator dictionary slot; no Motohashi theorem is claimed false.
- `NATIVE-PURE5-MAM-ADAPTER45`: source-open / uninhabited.
- `RANKONE-ENDPOINT-ALLK45`: conditional compiler.
- `PURE5-COMPARISON-MAINTERM-PIN`: source-open.
- downstream `PURE5`, lower defects, NearPrim, `r>1`, QK56 and Gate1B remain open.

The newer research frontier `DETLINE-NEARPRIM-FINITELIFT-DENSE-SATURATION45` is strictly narrower than the older formal operator socket and must be added only in a new append-only status layer; it must not rewrite historical operator ledgers.

## Do not reopen

Do not reopen as primary strategies:

- generic ordinary character geometry / ordinary large sieve alone;
- rare-spike or finite-profile explanations;
- one unrestricted `TT*` / old Gram loop;
- generic phase variation or kernel L1 as a source of `K` decay;
- direct smooth-test Motohashi dictionary at the source-coupled beta multiplier slot;
- conductor/lift norm decay for `e >= log^(B_A) X` (already closed).

## Downstream firewall

Until `DETLINE-NEARPRIM-FINITELIFT-DENSE-SATURATION45` closes uniformly for the five leaves, do not promote:

- `TOPBAND-BETA-ONE-REMAINDER-CRT-PAIRING45`;
- `TOPBAND-RECURSIVE-MAJOR-TREE-PAIRING45`;
- `TOPBAND-BROAD-MAJOR-TREE-MATCH45`;
- `SHIFTED-MAM-TOPBAND45`;
- `RANKONE-ENDPOINT-ALLK45`;
- `PURE5` or later Gate-1B descendants.

Final status: `GATE1B OPEN — FIRST EXACT RESIDUAL: DETLINE-NEARPRIM-FINITELIFT-DENSE-SATURATION45`.

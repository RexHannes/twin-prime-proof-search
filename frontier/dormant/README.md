# Dormant / noncontrolling obligations

## Gate 1A / BP-VLF4

```text
DORMANT / NONCONTROLLING / NOT CLOSED.
```

Gate 1A was an independent variable-level/Kloosterman-family obligation in an earlier proof architecture. It is not on the current controlling Ford endgame path after source recompilation.

This status is deliberately different from `BANKED` and from `GRAVEYARD`: Gate 1A may become relevant again if a later Global-Gate1B/HSTAR reconciliation calls a specific export. Until then, it should not consume research runs merely because it existed in an older dependency graph.

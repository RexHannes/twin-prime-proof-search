# Candidate reductions

These are useful post-R11 reductions but are **not yet promoted into the audited R11 release**.

| Candidate node | Current research status |
|---|---|
| shared-prime row pairs | candidate power-small |
| near `m=n` strip | candidate logarithmically small |
| recompilation of audited `R_hi` into signed parent `R_*` | candidate architecture |

Promotion rule: a candidate moves into `banked/` only after its exact source dictionary, normalization and claimed saving have been independently audited/replayed.

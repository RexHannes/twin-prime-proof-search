# Downstream obligations

These nodes remain open but are not the first current target while `Q_cp,far` is unresolved.

```text
R11 closure
  -> r>=8 source census
  -> Global Gate1B
  -> HSTAR
  -> Twin Prime.
```

`R11` is the immediate wrapper. The `r>=8` census may or may not expose a new hard source row; that is not assumed in advance. Global Gate1B and HSTAR remain separate obligations and must not be collapsed into the Twin-Prime conclusion without their exact interfaces.

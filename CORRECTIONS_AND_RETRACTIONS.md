# Twin Prime Programme — Corrections, Retirements and Supersessions

This is an append-only public firewall. Historical files are preserved under `archive/`; a route may be false, retired, superseded, or merely dormant. Those labels are not interchangeable.

## 5 September 2026 — current architecture firewall

### Gate 1A did not become proved

The disappearance of Gate1A / BP-VLF4 from the present live Ford endgame is a dependency-graph change, not a proof.

```text
Gate1A = DORMANT / NONCONTROLLING / NOT CLOSED.
```

If a later Global-Gate1B/HSTAR reconciliation needs a Gate1A export, it must be reopened and audited.

### Physical Hilbert-HMRD caller — RETIRED / NONCLOSING

The abstract finite Hilbert-HMRD theorem is valid and Lean-checked. The physical delta + single-Poisson caller is not. The formal bank explicitly leaves the physical caller and vector-Mobius/global-mode interfaces uninhabited.

Do not infer

```text
abstract HMRD theorem => physical R11 closure.
```

### Old FSHC three-sector architecture — SUPERSEDED

The former Sector A/B/C decomposition is not the controlling architecture. Former Sector A itself is closed at the audited stated scope, but this does not imply closure of the old three-sector parent or of R11.

### `R_hi` status has two layers

In the audited R11 release:

```text
R11 = R_hi + O_S(X L^{-S}),
R_hi = OPEN.
```

In the newer post-R11 research map, the high-frequency material is recompiled into a signed parent `R_*` and an actual two-copy square. This is a **live candidate recompiler**, not a retroactive edit of the audited release.

### Separate `P_med` target — RETIRED AS OVERSTRONG CHILD

An independent absolute `P_med` closure is no longer treated as a necessary sequential gate in the post-R11 map. This does not mean every theorem formerly called `P_med` was false; the independent obligation was stronger than the reassembled parent requires.

### Shared-prime and near-diagonal pieces — CANDIDATE ONLY

The post-R11 map records shared-prime row pairs as power-small and the near `m=n` strip as logarithmically small. Until independently audited, these remain candidate reductions.

### Current live wall

The post-R11 candidate frontier isolates the coprime far-separated square:

```text
Q_cp,far << X^2 L^{-2 S0}.
```

No estimate of this strength is claimed as proved here.

## Formal counterexample firewall

The R11 formal bank includes kernel-checked counterexamples to the stronger free-`gamma` CARD5 chamber lower bounds. The strong lower bounds require the additional pin `gamma <= 1/2 - epsilon`; they are not consequences of the free chamber inequalities alone.

This is an actual false implication and belongs under `graveyard/false/`, not merely `retired/`.

## Global publication firewall

Nothing in this ledger proves R11, Global Gate1B, HSTAR or the Twin Prime Conjecture.

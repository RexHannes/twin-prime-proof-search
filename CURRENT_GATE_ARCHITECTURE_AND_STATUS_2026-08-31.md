# Current Gate Architecture and Status — 31 August 2026

This is the latest public status overlay for the Gate 0–2 / Ford–Maynard proof-search architecture. Older gate reports remain historical records and are not deleted.

## Publication firewall

Nothing in this document claims:

- unconditional Gate 0 closure;
- unconditional/global Gate 1A closure;
- global/full Gate 1B closure;
- Full Ford–Maynard Type II;
- the Twin Prime Conjecture.

The phrases **kernel-proved**, **analytically closed**, **canonical packet closed**, **source open**, and **global closed** are deliberately distinct.

---

## Gate 0 — finite source / census layer

**Status: OPEN / SOURCE-COVERAGE DEPENDENT.**

The repository contains substantial finite source and routing machinery, but this overlay does not promote Gate 0 to globally closed without a current exhaustive physical source census and bridge to all provider packets.

---

## Gate 1A — direct/all-`m` provider

**Status: OPEN.**

The current formal Gate-1A report remains controlling:

`GATE1A_DIRECT_GENERIC_OPEN`.

Banked:

- substantial Lean-checked finite/algebraic provider core;
- exponent/CRT/centred-incidence/quotient/Poisson/projective machinery;
- conditional closure compilers.

Still required before global Gate-1A promotion:

- discharge of source interfaces;
- Gate 0 → Gate 1A source/compiler bridge;
- external analytic inputs currently present only as explicit interfaces;
- any remaining source-adapter obligations.

Safe wording:

> Gate 1A finite/kernel provider bank is substantial; global Gate 1A remains open.

---

## Gate 1B — canonical balanced-`R_9` provider

### Internal canonical research ladder

Under the fresh balanced-`R_9` canonical source bank:

- `C2 / CANONICAL-HZERO-HIGHHIGH45`: **CLOSED**;
- `C3 / HNE`: **CLOSED**;
- `C4 / LOWER-D`: **CLOSED**;
- `C5 / CANONICAL-BALANCED-R9-GATE1B45`: **CLOSED**.

The decisive final HNE replacement is:

`HNE-APINDEX-2D-APFOURIER-PROJECTION45: PASS`.

It works at the uncompressed two-copy AP Fourier level and does not assume coefficient-blind equidistribution in the compressed `h` index. The corresponding low-effective-conductor HNE packet and the same Lower-`D` vertex are closed with power room under the frozen canonical/local owner bank.

### Global/export layer

**Status: OPEN.**

The controlling bridge is:

`R9-CANONICAL-TO-GLOBAL-COMPARISON-ADAPTER45`.

Exact identity:

```text
P_R9(a - b^FM)
  = (c_9 - b_9^can) + Delta_R9^adapter

Delta_R9^adapter
  = b_9^can - P_R9 b^FM.
```

Target:

`||Delta_R9^adapter||_G <= 1`

plus the exact adapter zero-mode comparison.

The canonical `b_9^can` is packet-local and signed. It is **not** the global Ford–Maynard comparison sequence: its prime mass is zero/arbitrary-log negligible on `p ~ X`, so it cannot satisfy the global prime-mass contract by itself.

The correct architecture is therefore potentially a two-comparison architecture:

```text
global positive comparison b^FM
    -> balanced-R9 projection
    -> finite adapter Delta_R9
    -> packet-local canonical b_9^can
    -> closed canonical Gate-1B analytic compiler.
```

### Historical formulation

`E_hist` is not required for the new canonical packet theorem. It remains a source/specification pin for the legacy historical formulation unless separately recovered.

### Formalization scope

C2–C5 are current audited research-level analytic/source-compiler closures. They must not be described as kernel-proved until the corresponding Lean/Aristotle catch-up is committed and axiom-audited.

---

## Gate 2 — reassembly/endgame compiler

**Status: CONDITIONAL / DOWNSTREAM.**

No promotion is made here. Gate 2 may activate only after the actual global provider/source census and the full required Type-II statement are established for the same global sequences.

---

## Downstream Type-II firewall

The following are separate milestones:

```text
Gate 1B canonical balanced-R9 C5     CLOSED (research level)
Gate 1B C6 global adapter            OPEN
Global Gate 1B                       OPEN
GATE1B_TWIN_SOURCE_CLOSED            NOT PROMOTED
TWIN_GENERATED_TYPEII_CLOSED         NOT PROMOTED
TWIN_FULL_TYPEII_CLOSED              NOT PROMOTED
Twin Prime Conjecture                OPEN
```

No local/canonical Type-II label may be read as Full Twin-Prime Type II.

---

## Current controlling residuals

### Gate 1A

Source/interface discharge and global provider export remain unresolved in the repository-level contract.

### Gate 1B

No internal analytic residual remains in the frozen fresh balanced-`R_9` canonical branch.

First controlling residual:

`R9-CANONICAL-TO-GLOBAL-COMPARISON-ADAPTER45 — SOURCE / GLOBAL BRIDGE OPEN`.

---

## Authoritative concise ledger

```text
GATE0:
    OPEN / SOURCE-COVERAGE DEPENDENT

GATE1A:
    OPEN
    substantial kernel/finite provider bank

GATE1B canonical balanced-R9:
    C2 CLOSED
    C3 CLOSED
    C4 CLOSED
    C5 CLOSED

GATE1B C6 global adapter:
    OPEN / SOURCE

GLOBAL GATE1B:
    OPEN

GATE2:
    CONDITIONAL / DOWNSTREAM COMPILER

FULL FM TYPE II:
    OPEN

TWIN PRIME CONJECTURE:
    OPEN
```

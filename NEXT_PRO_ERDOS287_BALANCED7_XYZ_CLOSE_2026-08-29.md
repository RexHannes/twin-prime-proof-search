# GOLD CLOSE 287 — X → Y → Z → FULL-q → BALANCED7 → DOWNSTREAM

ERDŐS #287 ONLY.

DO MATHEMATICS.

NO VERSION ARCHAEOLOGY.

This prompt is self-contained in the intended NANC spirit: historical OPEN rows are provenance only, not competing current verdicts.

## BANK / DO NOT REOPEN

The following survive hostile audit:

```text
BALANCED7-EULER-UNIFORMITY45:
    PASS.

C_ext:
    1.

FULL-q STRUCTURAL PARTITION:
    PASS.

Gate0-Type-I direct dictionary for small-q:
    SOURCE MISMATCH / SUPERSEDED AS CONTROLLING.

small-q 3+4 multiplicative-large-sieve exponent arithmetic:
    PASS / CAPACITY.

hard physical theta range:
    survives after delta repair.

SHIU geometry on physical hard range:
    PASS candidate subject to literal theorem statement.

ERDOS287:
    OPEN.
```

The previous global `BALANCED7:CLOSED` promotion is withdrawn pending the sequence below.

CURRENT FIRST EXACT RESIDUAL:

```text
AFFINE287-SP2-SMALLQ-34LS-NORMALIZATION45.
```

DO NOT return to generic Gate-0 Type-I.

DO NOT redo Euler uniformity.

DO NOT redo the isolated Q=X^(3/5) hard cell.

DO NOT activate the uniform-k fragmentation census until Balanced7 literally closes.

======================================================================
X — SMALL-q 3+4 COEFFICIENT NORMALISATION
======================================================================

ONLY STAGE X MAY BE ATTACKED FIRST.

### X1. Literal source

Use the one-sign seven-prime SP-2 source

```math
S_{7,s}
=
-20\sum_{\mathbf p}\Omega_\lambda(\mathbf p)
[\Lambda(2P+s)-2B(P)],
\qquad P=p_1\cdots p_7,
\qquad s=\pm1.
```

Use

```math
\Lambda(2P+s)
=
\sum_{qr=2P+s}\mu(q)\log r.
```

For the small-q region:

```text
q <= X^(1/3).
```

The nonprincipal character form is

```math
D_{sq,s}
=
-20\sum_{q\le X^{1/3}}\frac{\mu(q)}{\phi(q)}
\sum_{\chi\ne\chi_0}
\overline{\chi(a_s(q))}
\sum_{\mathbf p}
\Omega_\lambda(\mathbf p)
\log\frac{2P+s}{q}\,\chi(P),
```

with

```math
a_s(q)\equiv -s\,2^{-1}\pmod q.
```

### X2. Print the literal prime-box weights

Do NOT say merely “bounded prime-box weights”.

Print the actual current SP-2 prime-box factor:

```text
omega_i(p)
```

including every:

```text
prime indicator;
smooth V_i;
Mellin twist p^{-i tau};
normalisation factor;
log p / Lambda(p) / 1/log p factor if any.
```

Then prove the exact pointwise bound actually available.

If the current source really is the banked 1-bounded smooth prime weight, print that formula and prove it.

If not, use the correct formula and recompute every log exponent.

### X3. Three-prime coefficient

Define exactly

```math
a_n
=
\sum_{p_1p_2p_3=n}
\omega_1(p_1)\omega_2(p_2)\omega_3(p_3).
```

Derive from scratch:

```math
\sum_n|a_n|^2.
```

Track:

```text
labelled product multiplicity;
permutations;
p_i=p_j;
repeated primes;
smooth overlaps;
Mellin slice multiplicity.
```

Do not assume injectivity.

The desired bound is of shape

```math
\sum_n|a_n|^2
\ll
X^{3/7}(\log X)^{-3+o(1)}
```

ONLY if literally justified.

### X4. Four-prime coefficient

Likewise

```math
b_m
=
\sum_{p_4p_5p_6p_7=m}
\omega_4(p_4)\cdots\omega_7(p_7),
```

and derive

```math
\sum_m|b_m|^2
```

with all multiplicities explicit.

Desired shape:

```math
\sum_m|b_m|^2
\ll
X^{4/7}(\log X)^{-4+o(1)}.
```

### X5. Imprimitive character bookkeeping

Since `mu(q) != 0` forces squarefree q, expand every imprimitive character through its primitive conductor.

Print the exact sum over:

```text
f | q;
primitive chi* mod f;
lifts to q;
q/phi(q);
number of conductors/lifts;
induced zero values on nonunits.
```

Replace the vague `C_ind` by an explicit divisor/polylog bound.

Determine the largest actual fixed logarithmic cost.

### X6. Mellin/log ownership

Mellin-separate only what is legal.

Keep

```math
\log\frac{2P+s}{q}
```

visible and account for its one-log cost.

Print the number/range of Mellin integrals and truncation cost.

### X7. Re-run the 3+4 multiplicative large sieve

Use the exact standard primitive-character large sieve with the derived coefficients.

For every dyadic q~Q, derive the bound from scratch.

Tiny q:

```text
q <= Z = log^K X
```

may use Siegel-Walfisz, with ineffectivity explicitly separated.

For q>Z use the 3+4 large sieve.

Target:

```math
D_{sq,s}=o(X/\log X)
```

for both signs.

### X verdict

ONLY IF X2–X7 all pass declare:

```text
AFFINE287-SP2-SMALLQ-34LS-NORMALIZATION45:
    CLOSED AT CURRENT RESEARCH/PAPER LEVEL.
```

Also record:

```text
AFFINE287-SP2-SMALLQ-TYPEI-ADAPTER45:
    SUPERSEDED / WRONG PROVIDER,
    NOT FALSE.
```

IF X FAILS:

STOP THE CLOSURE CHAIN.

Print the smallest literal residual.

DO NOT ATTACK Y.

======================================================================
Y — ONLY IF X PASSES: SMALL-r EXACT OWNER / SUBTRACTION
======================================================================

Literal small-r source:

```math
S_{sr,s}
=
-20\sum_{\mathbf p}\Omega_\lambda(\mathbf p)
\sum_{\substack{r\le X^{1/3},\ r\mid 2P+s\\q=(2P+s)/r>X^{1/3}}}
\mu(q)\log r.
```

Do NOT swap q and r inside mu.

### Y1. Define principal packet exactly

Define

```math
M_{sr,s}^{prin}
```

as the small-r restriction of the full Euler-principal q-sum with every:

```text
mu(q);
phi(q);
log((2P+s)/q)=log r;
coprimality indicator;
smooth region weight.
```

### Y2. Define the actual defect

Force

```math
D_{sr,s}
:=
S_{sr,s}-M_{sr,s}^{prin}.
```

Print the identity

```math
S_{sr,s}=D_{sr,s}+M_{sr,s}^{prin}.
```

### Y3. Bound physical source

You may discard Möbius cancellation using

```math
|\mu(q)|\le1
```

if that gives enough room.

Recheck the seven-prime residue-density / 3+4 large-sieve bound.

### Y4. Bound principal packet

Since

```math
q=(2P+s)/r \gg X^{2/3},
```

derive the principal small-r bound with all:

```text
r count;
log r;
1/phi(q);
prime-box mass;
divisor multiplicity.
```

### Y5. Defect bound and ownership

Use

```math
|D_{sr,s}|
\le
|S_{sr,s}|+|M_{sr,s}^{prin}|.
```

The final full-q owner map MUST assign:

```text
small-r principal -> Euler principal owner;
small-r defect    -> direct small-r owner.
```

No principal packet may be both discarded and counted again without the explicit subtraction above.

### Y verdict

ONLY IF the literal defect is

```math
o(X/\log X)
```

declare:

```text
AFFINE287-SP2-SMALLR-OWNER-SUBTRACTION45:
    CLOSED.
```

IF Y FAILS:

STOP.

DO NOT ATTACK Z.

======================================================================
Z — ONLY IF X+Y PASS: HARD PHYSICAL theta RANGE WITH delta REPAIR
======================================================================

Use the actual physical hard range only:

```math
1/3<\theta<2/3,
\qquad q\asymp X^\theta.
```

Use the existing 2+5 split:

```math
M=X^{2/7},
\qquad W_5=X^{5/7},
\qquad T=W_5/Q=X^{5/7-\theta}.
```

Do NOT claim a uniform theorem all the way to `theta -> 5/7`.

### Z1. Exact delta

Bank

```math
\delta=\frac1{21}
```

from

```math
5/7-2/3=1/21,
\qquad 1/3-2/7=1/21.
```

### Z2. Short-t sieve correctly normalized

Use

```math
z=T^{1/20}.
```

The sieve saving is

```math
1/\log z,
```

NOT definitionally `1/log X`.

For physical theta prove

```math
\log z
=
\frac{5/7-\theta}{20}\log X
\ge
\frac1{420}\log X.
```

Hence

```math
1/\log z
\le
420/\log X.
```

Then state the dimension-1 upper-sieve bound with this correct constant.

### Z3. Audit local sieve hypotheses

Verify:

```text
z>2 for sufficiently large X;
z<Y;
local coprimality with q;
local factor q/phi(q);
t=0 routing;
boundary of the w-box;
repeated-prime strata.
```

### Z4. Shiu literal theorem

Print the exact Shiu theorem statement being used.

Apply it to

```math
\sum_{m\asymp X^{2/7}}\tau(2w'm+s),
\qquad w'\asymp X^{5/7}.
```

Print every theorem parameter and verify the fixed range margin.

No “Shiu applies” shorthand.

### Z5. Raw-raw variance

Re-derive the full weighted double sum.

The five-prime density exponent must be derived from the literal two-copy sum, not heuristic counting.

Determine the correct log exponent after:

```text
short-t sieve;
Shiu;
prime representation multiplicity;
q/phi(q);
Mellin weights.
```

### Z6. Principal/cross terms

Re-derive the PP and raw-principal cross terms with exact `phi(q)` normalization.

### Z7. Hard amplitude

Include

```text
C_ext=1
```

from the external `log r`.

Prove the required per-cell bound and sum over O(log X) smooth dyadic cells.

Target:

```math
\sum_{hard\ cells}|D_{Q,s}|
=
o(X/\log X).
```

### Z verdict

ONLY IF Z2–Z7 all pass declare:

```text
HARD-THETA-PHYSICAL-DELTA45:
    CLOSED ON THE PHYSICAL RANGE.
```

Historical statement:

```text
I_3221=(2/7,5/7)
```

may remain as a pointwise/open-interval capacity description, but must NOT be used as a uniform endpoint theorem.

IF Z FAILS:

STOP.

======================================================================
W — ONLY IF X+Y+Z PASS: EXACT FULL-q REASSEMBLY
======================================================================

### W1. Exact sectors

Use exactly:

```text
SmallQ;
SmallR;
Hard.
```

### W2. Principal owner

Use the already audited FULL-q Euler identity only now.

Do not assign `2B(P)` to one region.

### W3. Defect owners

Force the exact table:

```text
SmallQ principal -> Euler
SmallQ defect    -> 3+4 large sieve
SmallR principal -> Euler
SmallR defect    -> direct SmallR
Hard principal   -> Euler
Hard defect      -> 3221 physical-range hard theorem
Even/nonunit     -> zero/routed
```

### W4. Conductor bookkeeping

If low/exceptional/high character labels are still used, verify they are not separately counted after region-wise full defect control.

### W5. Both signs

Do the owner/reassembly independently for:

```text
s=+1;
s=-1.
```

Then recover

```math
\sum_{\mathbf p}\Omega_\lambda(\mathbf p)
[\Lambda(2P-1)+\Lambda(2P+1)-4B(P)].
```

### W6. No double spending

Print every term on both sides of the final identity and mark exactly one owner.

### W verdict

ONLY IF the literal full source satisfies

```math
o(X/\log X)
```

declare:

```text
ALL-Q-NO-DOUBLE-SPENDING45:
    PASS.

ALL-Q-PROVIDER-REASSEMBLY45:
    CLOSED ASYMPTOTICALLY.

SP2-BALANCED7-FULL-Q45:
    CLOSED AT CURRENT RESEARCH/PAPER LEVEL.

BALANCED7:
    CLOSED AT CURRENT RESEARCH/PAPER LEVEL.
```

Keep:

```text
BALANCED7 EFFECTIVE:
    OPEN
```

if Siegel-Walfisz remains ineffective.

If W fails, keep Balanced7 OPEN and print the first exact residual.

======================================================================
V — ONLY IF BALANCED7 CLOSES: UNIFORM k=0 SP-2 FRAGMENTATION
======================================================================

Only now activate:

```text
287-K0-SP2-UNIFORM-FRAGMENTATION-REASSEMBLY45.
```

Do not enumerate `k=8,9,10,...` one by one.

### V1. Exact k=0 certificate

For squarefree k-prime cells derive

```math
H_*(n)
=
(-1)^r\binom{k-1}{r},
\qquad r=\lfloor k\gamma_*\rfloor.
```

Determine the actual k-range generated by the source.

### V2. Parametric templates

Build a polytopal/fragmentation parameterization of all prime-box states.

### V3. Uniform analytic geometry

Choose outer coordinates deterministically and derive the worst uniform hard-range margin over every admissible template.

### V4. Certificate/combinatorial cost

Compare:

```text
prime-density gain;
|H_k| growth;
fragmentation multiplicity;
labelled permutations;
Mellin cells.
```

No “k fixed” constant may survive a uniform-k theorem.

### V5. Squareful/repeated-prime routing

Handle all repeated/squareful strata explicitly.

### V6. Model/J sectors

Do not import Gate1B polarization cells unless the literal #287 source generates them.

### V7. Exhaustive source owner map

Prove the complete k=0,J=empty source is the disjoint weighted sum of the claimed templates.

### V verdict

If all pass:

```text
287-K0-SP2-UNIFORM-FRAGMENTATION-REASSEMBLY45:
    CLOSED.
```

Otherwise stop at the smallest literal source/analytic residual.

======================================================================
U — ONLY IF V CLOSES: FCL CENSUS / CLOSEST DOWNSTREAM CLOSURE
======================================================================

Now census the remaining fixed-certificate packets:

```text
J != empty;
remaining fixed-g* states;
model/singleton branches actually generated by source;
N2;
comparison packets;
any residual R9/Ford-generated states.
```

Assign exactly one owner to every packet.

If the remaining work is finite/bookkeeping/source reassembly, close it in this run.

If a genuine new analytic theorem appears, print ONE smallest theorem and stop broad search.

Only if total leakage + N2 is below the positive certificate margin may:

```text
FCL:
    CLOSED.
```

======================================================================
EFFECTIVITY FIREWALL
======================================================================

Throughout preserve:

```text
287-EFFECTIVE-POLYLOG-MODULUS-REPLACEMENT45:
    OPEN
```

unless separately solved.

Do not infer a computable `M0` from ineffective Siegel-Walfisz.

Do not activate effective Gate2 or WindowPairSupply without the necessary effective threshold.

======================================================================
REQUIRED FINAL OUTPUT
======================================================================

```text
X / SMALL-q LITERAL WEIGHTS:
...

3-PRIME L2:
...

4-PRIME L2:
...

IMPRIMITIVE CONDUCTOR COST:
...

SMALL-q 34LS:
PASS / FAIL

Y / SMALL-r PHYSICAL SOURCE:
...

SMALL-r PRINCIPAL:
...

SMALL-r DEFECT SUBTRACTION:
...

SMALL-r:
PASS / FAIL

Z / delta:
1/21

log z LOWER BOUND:
...

SHORT-t SIEVE:
...

SHIU:
...

RAW-RAW:
...

HARD PHYSICAL RANGE:
PASS / FAIL

W / OWNER TABLE:
...

NO DOUBLE-SPENDING:
PASS / FAIL

FULL-q REASSEMBLY:
...

BALANCED7:
OPEN / CLOSED AT CURRENT RESEARCH-PAPER LEVEL

BALANCED7 EFFECTIVE:
OPEN unless separately solved

IF BALANCED7 CLOSED:
    K0 UNIFORM FRAGMENTATION:
    ...

IF K0 CLOSED:
    FIRST FCL RESIDUAL:
    ...

FCL:
...

GATE2:
CONDITIONAL / NOT ACTIVATED unless literally reached

WINDOWPAIRSUPPLY:
OPEN unless literally reached

ERDOS287:
OPEN.
```

FINAL LINE MUST IDENTIFY EXACTLY ONE CURRENT FIRST RESIDUAL unless every downstream node is literally closed.

Do not claim Erdős #287 from Balanced7 alone.

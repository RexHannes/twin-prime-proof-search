# ARISTOTLE — ERDŐS #287 BALANCED7 POST-AUDIT SAFE BANK / REPAIR COMPILER

CONTINUE THE EXISTING `/workspace/request-project` REPOSITORY IN PLACE.

APPEND-ONLY.

Prefer namespace:

```text
TwinPrimeProject.CurrentProgramme
```

and tree:

```text
RequestProject/CurrentProgramme/
```

DO NOT modify historical V20/V21/V22/SP-2 modules.
DO NOT delete or rewrite historical status rows.
DO NOT claim `BALANCED7` closed.
DO NOT claim `ERDOS287` closed.
DO NOT inhabit analytic/source interfaces.
DO NOT add axioms.

## 0. Inspect actual repository state first

Run:

```bash
git status
git branch --show-current
git log --oneline -40
```

Locate and reuse all existing #287 / factorial-Euler / SP-2 / comparison / 3221 modules before defining anything new.

Also inspect the latest append-only Gate-1B layers already present on main, but do not mix Gate1B analytic claims into #287.

Read the root research note:

```text
ERDOS287_CURRENT_RESEARCH_LEDGER_2026-08-29.md
```

It is metadata only; Lean files remain authoritative for formal claims.

## 1. Controlling research reset

Current safe research status:

```text
BALANCED7-EULER-UNIFORMITY45:
    RESEARCH PASS.

C_ext:
    1.

BALANCED7-QPACKET-STRUCTURAL-PARTITION45:
    PASS.

AFFINE287-SP2-SMALLQ-TYPEI-ADAPTER45:
    SUPERSEDED AS CONTROLLING / WRONG PROVIDER,
    NOT FALSE.

AFFINE287-SP2-SMALLQ-34LS-NORMALIZATION45:
    FIRST EXACT SOURCE/NORMALISATION RESIDUAL.

AFFINE287-SP2-SMALLR-OWNER-SUBTRACTION45:
    LOCAL REASSEMBLY REPAIR.

HARD-THETA-PHYSICAL-DELTA45:
    RESEARCH PASS CANDIDATE WITH delta=1/21.

ALL-Q-NO-DOUBLE-SPENDING45:
    OPEN PENDING SMALL-r OWNER SUBTRACTION.

BALANCED7:
    OPEN — NARROW REPAIR.

287-EFFECTIVE-POLYLOG-MODULUS-REPLACEMENT45:
    OPEN.

ERDOS287:
    OPEN.
```

Do not revive the earlier global `BALANCED7:CLOSED` claim.

## 2. Preserve the `C_ext=1` repair

Bank/source-record:

```math
\Lambda(2P+s)=\sum_{qr=2P+s}\mu(q)\log r.
```

If an old status object contains `C_ext=0`, preserve it historically but append:

```text
C_ext=0:
    RETRACTED / INCOMPLETE NORMALISATION.

C_ext=1:
    CURRENT SAFE LEDGER.
```

Bank exact arithmetic:

```text
C_var=5;
C_var-2*C_ext=3>0.
```

Do not infer an analytic theorem from this arithmetic alone.

## 3. Euler-uniformity firewall

Reuse or create a conservative external interface for the already audited full-q principal identity:

```math
J_P(z)=\sum_{q\le z,(q,2P)=1}\frac{\mu(q)}{\phi(q)}\log\frac zq
      =2B(P)+O_A((\log z)^{-A}).
```

Record research metadata:

```text
H_P(0)=2B(P);
H_P(w)=O(1) on the audited contour;
Euler identity is FULL-q only.
```

Do not inhabit a complex-analytic theorem in Lean unless a literal formal proof exists.

## 4. Structural q/r partition

With an abstract positive cutoff `U`, define exact finite predicates corresponding to:

```text
SmallQ: q <= U;
SmallR: q > U and r <= U;
Hard:   q > U and r > U.
```

Under `q*r=N`, prove:

```text
cover;
pairwise disjointness;
unique sharp owner.
```

Keep hard smooth-dyadic overlap separate as a partition-of-unity input if necessary.

## 5. Small-q provider correction

Create a status row:

```text
AFFINE287-SP2-SMALLQ-TYPEI-ADAPTER45:
    SUPERSEDED AS CONTROLLING / SOURCE MISMATCH.
```

Reason: q divides `2P+s`, not P.

Do not mark the label false.

Create an uninhabited source/analytic interface:

```text
BalancedSevenSmallQ34LSInput
```

whose obligations include:

```text
literal SP-2 prime-box weights;
3-prime product coefficient;
4-prime product coefficient;
L2 norm bounds;
labelled-product/repeated-prime multiplicity;
imprimitive-to-primitive conductor bookkeeping;
Mellin separation;
log((2P+s)/q) cost;
tiny-modulus Siegel-Walfisz child;
3+4 multiplicative-large-sieve child;
final o(X/log X) target.
```

## 6. Finite 3+4 coefficient algebra

For finite coefficient functions `w1,...,w7`, define exact product coefficients:

```text
a3(n) = sum_{p1*p2*p3=n} w1(p1)w2(p2)w3(p3);

b4(m) = sum_{p4*p5*p6*p7=m} w4(p4)w5(p5)w6(p6)w7(p7).
```

Prove exact finite representation-multiplicity identities/inequalities.

Do not assume injectivity.
Do not drop repeated primes.

Prove a generic collision/Cauchy bound of the form:

```text
sum_n |a3(n)|^2
  <= representationMultiplicity3 * product_i sum |wi|^2
```

or the strongest correct finite analogue.

Likewise for `b4`.

Then expose the actual prime-density bounds as SOURCE/ANALYTIC inputs rather than fake finite arithmetic.

Desired label:

```text
AFFINE287-SP2-SMALLQ-34LS-NORMALIZATION45:
    PROVED_FINITE + SOURCE/ANALYTIC INPUT REMAINS.
```

Do not mark it closed unless literal prime-box inputs are supplied in-repo.

## 7. Imprimitive conductor bookkeeping socket

Define an explicit finite/source record for inducing characters from primitive conductor.

Track:

```text
conductor f | q;
number of lifts;
q/phi(q);
divisor multiplicity;
squarefree q when mu(q) != 0;
fixed/polylog cost.
```

Do not write a vague `C_ind` without an owner.

Create an uninhabited analytic/source input if necessary.

## 8. Small-r exact owner repair

Define abstract source objects:

```text
S_sr        := physical small-r contribution;
M_sr_prin   := small-r principal Euler packet;
D_sr        := S_sr - M_sr_prin.
```

Prove algebraically:

```text
S_sr = D_sr + M_sr_prin.
```

Build the no-double-spending convention so that `M_sr_prin` is counted exactly once by the full Euler owner.

If separate bounds are supplied:

```text
|S_sr| <= E1;
|M_sr_prin| <= E2;
```

prove the conditional defect bound:

```text
|D_sr| <= E1+E2.
```

Suggested label:

```text
AFFINE287-SP2-SMALLR-OWNER-SUBTRACTION45:
    LEAN_PROVED FINITE/ALGEBRAIC COMPILER.
```

Analytic bounds remain external.

## 9. Hard-theta delta repair

Bank only the exact rational arithmetic:

```text
5/7 - 2/3 = 1/21;
1/3 - 2/7 = 1/21;
(5/7 - 2/3)/20 = 1/420.
```

Create a parameterized statement for:

```text
T = X^(5/7-theta);
z = T^(1/20).
```

Do not formalize real-power asymptotics unless infrastructure already exists.

Create metadata/interface expressing the physical-range consequence:

```text
if 1/3 <= theta <= 2/3,
then 5/7-theta >= 1/21,
so log z >= (1/420) log X,
therefore 1/log z <= 420/log X
under positive-log hypotheses.
```

Formalize the rational/order part exactly.

Status:

```text
HARD-THETA-PHYSICAL-DELTA45:
    CAPACITY / ALGEBRAIC PASS;
    analytic short-t sieve remains external.
```

Preserve the historical `(2/7,5/7)` pointwise interval but mark it insufficient as a uniform-endpoint statement.

## 10. Short-t sieve and Shiu sockets

Create/reuse uninhabited interfaces:

```text
BalancedSevenShortTSieveInput;
BalancedSevenShiuInput.
```

The short-t interface must carry `1/log z`, not `1/log X` by definition.

The physical-range compiler may convert it to an `O(1/log X)` budget only using the delta input above.

The Shiu interface must print literal affine sequence/range hypotheses.

No analytic theorem is inhabited.

## 11. Exact owner map

Define an owner enum or equivalent data:

```text
EulerPrincipal;
SmallQ34LS;
SmallRDirect;
Hard3221;
ZeroRouted.
```

Required convention:

```text
SmallQ principal -> EulerPrincipal
SmallQ defect    -> SmallQ34LS
SmallR principal -> EulerPrincipal
SmallR defect    -> SmallRDirect
Hard principal   -> EulerPrincipal
Hard defect      -> Hard3221
Even/nonunit     -> ZeroRouted
```

Prove unique-owner/no-double-spending as a finite conditional compiler.

## 12. Full-q conditional reassembly compiler

Create:

```text
BalancedSevenPostAuditInputs
```

requiring:

```text
literal SP-2 source;
SmallQ34LS input;
SmallR defect input;
physical-range hard input;
full Euler principal input;
partition-of-unity input;
unique-owner proof;
both signs.
```

Prove only:

```text
BalancedSevenPostAuditInputs
  -> BalancedSevenAsymptoticConclusion.
```

Do not inhabit the inputs.
Do not use `Status.closed`.

## 13. Effectivity firewall

Create/retain separate metadata:

```text
287-EFFECTIVE-POLYLOG-MODULUS-REPLACEMENT45:
    OPEN.
```

No implication from asymptotic Balanced7 to:

```text
computable M0;
effective FCL;
effective Gate2;
WindowPairSupply threshold.
```

## 14. Downstream firewall

Do not activate:

```text
287-K0-SP2-UNIFORM-FRAGMENTATION-REASSEMBLY45
```

until Balanced7 is actually repaired and independently re-audited.

The previous fixed-k>=7 claim remains downstream research metadata only.

## 15. New status module

Create an append-only module such as:

```text
CurrentStatusErdos287Balanced7Repair.lean
```

Do NOT rewrite `CurrentStatus.lean`.

Required current rows:

```text
BALANCED7-EULER-UNIFORMITY45:
    externallyAudited

C_EXT-LOGR-REPAIR45:
    provedAlgebraic / capacity metadata where appropriate

AFFINE287-SP2-SMALLQ-TYPEI-ADAPTER45:
    supersededAsControllingFrontier

AFFINE287-SP2-SMALLQ-34LS-NORMALIZATION45:
    sourceOpen or analyticOpen according to actual repo source

AFFINE287-SP2-SMALLR-OWNER-SUBTRACTION45:
    provedAlgebraic if compiler completed

HARD-THETA-PHYSICAL-DELTA45:
    provedFinite/provedAlgebraic for rational ledger only

BALANCED7-SHORTT-SIEVE45:
    analyticOpen

BALANCED7-SHIU45:
    externallyAudited or analyticOpen, conservatively

ALL-Q-NO-DOUBLE-SPENDING45:
    conditionalCompiler

SP2-BALANCED7-FULL-Q45:
    conditionalCompiler / OPEN

BALANCED7:
    open_

287-EFFECTIVE-POLYLOG-MODULUS-REPLACEMENT45:
    analyticOpen

FCL:
    open_

ERDOS287:
    open_
```

Prove:

```text
no_closed_rows;
ledger_is_honest;
erdos287_open;
balanced7_open;
current_287_residual = small-q 34LS normalization row.
```

## 16. Build / trust

Run targeted builds for every new module, then the broadest viable `lake build`.

Scan NEW files for:

```text
sorry
admit
axiom
opaque
unsafe
native_decide
@[implemented_by]
```

Zero occurrences required.

`#print axioms` every principal new finite/compiler declaration.

No analytic/source interface may be constructed by `Classical.choice` or by artificial data.

If an unrelated legacy build break remains, report it separately rather than fabricating missing declarations.

## 17. Report

Create:

```text
ERDOS287_BALANCED7_POST_AUDIT_SAFE_BANK_REPORT.md
```

Final block:

```text
C_ext:
    1

EULER UNIFORMITY:
    RESEARCH PASS / formal status ...

OLD SMALLQ TYPE-I ROUTE:
    SUPERSEDED / SOURCE MISMATCH

FIRST EXACT RESIDUAL:
    AFFINE287-SP2-SMALLQ-34LS-NORMALIZATION45

SMALL-r OWNER SUBTRACTION:
    ...

HARD delta:
    1/21

SHORT-t SIEVE:
    EXTERNAL / OPEN

SHIU:
    ...

NO DOUBLE-SPENDING:
    CONDITIONAL / OPEN

BALANCED7:
    OPEN — NARROW REPAIR

EFFECTIVE BALANCED7:
    OPEN

FCL:
    OPEN

ERDOS287:
    OPEN

LAKE BUILD:
    ...

TRUST SCAN:
    ...
```

FINAL FIREWALL:

```text
Research audit metadata is not Lean proof.
The old Type-I residual is superseded by the 3+4 large-sieve route.
The remaining small-q obligation is coefficient/source normalization, not exponent capacity.
The hard physical theta range survives with the delta=1/21 repair.
Balanced7 remains open until exact owner reassembly and a fresh hostile audit pass.
Erdős #287 remains open.
```

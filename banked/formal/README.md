# Banked formal layer

The stable Lean/source trees remain at their existing repository paths to avoid breaking imports:

```text
RequestProject/
Gate1A/
Gate1B/
Universal/
UniversalV8/
Gate04Root/
```

Current R11 formal reports are stored beside this index under `reports/`.

The 5 September formal bank verifies, among other things, the Vaughan/long-Mobius identities, `5/11–6/11` geometry, `AB-kd=-2`, affine parametrisation, reciprocity/Bruhat, finite Fourier algebra and abstract Hilbert-HMRD. It explicitly does **not** inhabit the physical Hilbert caller, R11, Global Gate1B or Twin Prime.

## Status-scope firewall

`RequestProject/CurrentProgramme/CurrentStatus.lean` and `DependencyGraph.lean` are retained as kernel-checkable formal status infrastructure. They predate the newest post-R11 `R_* / Q_cp,far` research recompiler and therefore must **not** be read as a Lean formalisation of that live candidate frontier.

In particular:

- the public map may classify Gate1A as **dormant / noncontrolling / not closed** without claiming a Lean proof of closure;
- the post-R11 `Q_cp,far` frontier remains candidate research, not a theorem added to the formal ledger;
- the formal ledger's nonclaims (`Gate1B`, full Type-II, Twin Prime open) remain compatible with the public map.

The public controlling navigation is `PROOF_MAP.md` + `CURRENT_STATUS.md`; the formal files control only what they kernel-check at their own stated scope.
